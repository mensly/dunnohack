rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /games/{code}/{document=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == get(/games/$(code)).data.host;
    }
    match /games/{code}/players/{playerId}/{document=**} {
      allow write: if request.auth != null && request.auth.uid == playerId;
    }
  }
}